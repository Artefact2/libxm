ADD_EXECUTABLE(xmtest xmtest.c)
TARGET_LINK_LIBRARIES(xmtest ${XM_LIBRARIES})

IF(${ALSA_FOUND})
ADD_DEFINITIONS(-D_POSIX_SOURCE)
ADD_EXECUTABLE(xmtoalsa xmtoalsa.c)
TARGET_LINK_LIBRARIES(xmtoalsa ${ALSA_LIBRARIES} ${XM_LIBRARIES})
ENDIF()

INCLUDE_DIRECTORIES(${XM_INCLUDE_DIRS})
